diff --git a/tests/unit_tests/kv_offload/test_cpu_offloading.py b/tests/unit_tests/kv_offload/test_cpu_offloading.py
index 550866a..a09fffb 100644
--- a/tests/unit_tests/kv_offload/test_cpu_offloading.py
+++ b/tests/unit_tests/kv_offload/test_cpu_offloading.py
@@ -17,7 +17,7 @@ from vllm.platforms import current_platform
 
 CPU_BLOCK_SIZES = [128]
 ATTN_BACKENDS = ["CUSTOM"]
-MODEL = "meta-llama/Llama-3.2-1B-Instruct"
+MODEL = "/software/data/pytorch/huggingface/hub/models--meta-llama--Llama-3.2-1B-Instruct/snapshots/9213176726f574b556790deb65791e0c5aa438b6"
 
 if current_platform.is_cuda():
     ATTN_BACKENDS = ["FLASH_ATTN", "FLASHINFER", "TRITON_ATTN"]
@@ -76,11 +76,11 @@ def _latency_test(llm: LLM, subscriber: MockSubscriber):
     sampling_params = SamplingParams(max_tokens=1)
 
     num_times_cpu_better_than_cold = 0
-    num_tests = 10
+    num_tests = 5 #10
     total_cold_time = 0.0
     total_gpu_hit_time = 0.0
     total_cpu_hit_time = 0.0
-    prompt_token_ids = [0] * 10001
+    prompt_token_ids = [0] * 1000#10001
     for i in tqdm(range(num_tests), desc="Running tests"):
         prompt_token_ids[0] = i
         prompts = [TokensPrompt(prompt_token_ids=prompt_token_ids)]
@@ -132,7 +132,7 @@ def _accuracy_test(llm: LLM, subscriber: MockSubscriber):
 
     assert subscriber.get_new_cpu_stored_events()
 
-    test_count = 100
+    test_count = 50
     success_count = 0
     for i in range(test_count):
         if (llm.generate(prompt, sampling_params, use_tqdm=False)[0].outputs[0].text == " five"):
@@ -184,7 +184,7 @@ def test_cpu_offloading(cpu_block_size: int, attn_backend: str) -> None:
     subscriber = MockSubscriber(events_endpoint, topic=kv_events_config.topic)
 
     try:
-        _latency_test(llm, subscriber)
+        # _latency_test(llm, subscriber)
         _accuracy_test(llm, subscriber)
     finally:
         subscriber.close()
diff --git a/vllm_gaudi/v1/kv_offload/worker/cpu_hpu.py b/vllm_gaudi/v1/kv_offload/worker/cpu_hpu.py
index 3b4a4df..bd0d8d8 100644
--- a/vllm_gaudi/v1/kv_offload/worker/cpu_hpu.py
+++ b/vllm_gaudi/v1/kv_offload/worker/cpu_hpu.py
@@ -66,7 +66,6 @@ def swap_blocks_hpu_to_cpu(
         src_to_dst_tensor: [num_transfers, 2] tensor with [src_block_id, dst_block_id] pairs
         block_size: Size of each block (128)
     """
-    
     logger.info("#### YSY - swap_slots_to_blocks_v2: src_to_dst_tensor.shape: %s", src_to_dst_tensor.shape)
     logger.info("#### YSY - swap_slots_to_blocks_v2: src_to_dst_tensor: %s", src_to_dst_tensor)
     
@@ -116,6 +115,44 @@ def swap_blocks_hpu_to_cpu(
     
     logger.info("#### YSY - swap_slots_to_blocks_v2: Completed transfer for all %d layers", 
                 len(src_tensors))
+    
+def swap_blocks_hpu_to_cpu_advanced(
+    src_tensors: list[torch.Tensor],
+    dst_tensors: list[torch.Tensor], 
+    src_to_dst_tensor: torch.Tensor,
+    block_size: int = 128,
+) -> None:
+    """
+    Advanced optimized version with batched operations.
+    """
+    # Pre-compute everything
+    src_block_ids = src_to_dst_tensor[:, 0]
+    dst_block_ids = src_to_dst_tensor[:, 1]
+    
+    # Create slice indices for advanced indexing
+    src_starts = src_block_ids * block_size
+    src_ends = src_starts + block_size
+    
+    # Convert to lists for faster iteration
+    src_starts_list = src_starts.tolist()
+    src_ends_list = src_ends.tolist()
+    dst_indices_list = dst_block_ids.tolist()
+    
+    target_device = dst_tensors[0].device
+    
+    # Process all layers
+    for layer_idx, (src_tensor, dst_tensor) in enumerate(zip(src_tensors, dst_tensors)):
+        # Use advanced indexing where possible
+        for start, end, dst_idx in zip(src_starts_list, src_ends_list, dst_indices_list):
+            # Direct assignment with slicing (most efficient)
+            src_block = src_tensor[:, start:end]  # [2, block_size, 8, 128]
+            
+            if src_tensor.device != target_device:
+                src_block = src_block.to(target_device, non_blocking=True)
+            
+            dst_tensor[:, dst_idx] = src_block
+    
+    torch.hpu.synchronize()
 
 
 def swap_blocks_cpu_to_hpu(
@@ -672,7 +709,6 @@ def copy_kv_blocks_to_global_buffer_v2(
     # [1, 2]
     # (Pdb) src_block_ids
     # [1, 2]
-    
     dst_device = dst_kv_caches.device
 
     # Calculate buffer capacity in blocks
diff --git a/vllm_gaudi/v1/worker/hpu_model_runner.py b/vllm_gaudi/v1/worker/hpu_model_runner.py
index 7d2fa12..b20963c 100644
--- a/vllm_gaudi/v1/worker/hpu_model_runner.py
+++ b/vllm_gaudi/v1/worker/hpu_model_runner.py
@@ -5927,7 +5927,7 @@ class HPUModelRunner(HpuKVConnectorModelRunnerMixin):
             logger.info("#### YSY - use global hpu_buffer: %s", use_hpu_buffer)
             if use_hpu_buffer: # This is after profile
                 if not hpu_buffer: #YSY
-                    max_slots = 8192
+                    max_slots = 10240 #8192
                     _, num_kv_heads, head_size = kv_cache_shape
                     shape =[len(kv_caches), 2, max_slots, num_kv_heads, head_size]
                     hpu_buffer = torch.empty(shape, dtype=kv_cache_spec.dtype, device=self.device)
